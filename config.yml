# Legacy config placeholder.
# This project now reads configuration from environment variables / `.env`.
# See README.md for the current setup.
bot:
  token: your_discord_token_here
  owner_ids:
    - 123456789012345678
  prefix: $
  default_bet: 100
  bonus_multiplier: 5
  bonus_cooldown: 12
